# === 大型原始截圖（可從 GPB 重抓的中間產物，160MB，不入版控）===
.nlm-pipeline/screenshots/

# === Python 快取 ===
__pycache__/
*.pyc
*.pyo
.ruff_cache/
.pytest_cache/
.venv/

# === 當機 / 系統垃圾檔（junk-files-gitignore 規則）===
*.stackdump
core
*.core
*.dmp
*~
*.swp
.DS_Store
Thumbs.db

# 機器生成的 session 快照（每輪對話自動刷新，不進版本控制）
.claude/session-state.md

# === PDF 便利複本（正本已在 .nlm-pipeline/ 版控中，PDF/ 只是本機方便取用，不重複入版控）===
PDF/
